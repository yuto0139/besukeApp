%h1 Comments

%table
  %thead
    %tr
      %th Body
      %th Status
      %th{ colspan: 3 }

  %tbody
    - @comments.each do |comment|
      %tr
        %td= comment.body
        %td= comment.status
        %td= link_to 'Show', blog_entry_comment_path(@blog, @entry, comment)
        %td= link_to 'Edit', edit_blog_entry_comment_path(@blog, @entry, comment)
        %td= link_to 'Destroy', blog_entry_comment_path(@blog, @entry, comment), method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Comment', new_blog_entry_comment_path(@blog, @entry)
