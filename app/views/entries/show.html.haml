%p#notice= notice

%h1= @blog.title

%p
  %strong Title:
  = @entry.title
%p
  %strong Body:
  = @entry.body

= link_to 'Edit', edit_blog_entry_path(@blog, @entry)
\|
= link_to 'Back', blog_path(@blog)

%hr

= render 'comments_index'
= render 'comments_form', blog: @blog, entry: @entry, comment: @new_comment
