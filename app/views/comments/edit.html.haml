%h1 Editing Comment

= render 'form', blog: @blog, entry: @entry, comment: @comment

= link_to 'Show', blog_entry_comment_path(@blog, @entry, @comment)
\|
= link_to 'Back', blog_entry_path(@blog, @entry)
